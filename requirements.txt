# Spinal Movement Risk Monitor — pinned environment
# Python 3.11 · scikit-learn 1.8.0 · fixed random seed 42 throughout
# Install:  pip install -r requirements.txt   (run from 03_code/ to resolve imports)

# Data acquisition
brainflow==5.20.1          # OpenBCI Ganglion/Cyton sEMG streaming
pyserial>=3.5              # IMU serial recording (breadboard + PCB, cabled)
bleak>=0.21               # Wireless IMU recording from the PCB over BLE

# Signal processing and ML
numpy>=1.24
scipy>=1.10
scikit-learn==1.8.0        # exact version — AUCs are reproducible only on this build
joblib==1.5.3              # model serialisation (required by scikit-learn)

# Data handling
pandas>=2.0

# Visualisation
matplotlib>=3.7

# Notebooks (optional, exploratory work only)
jupyter>=1.0
ipykernel>=6.0

# This consolidates the three working requirements files (core, dashboard,
# replay) into one. The replay dashboard and acquisition GUIs use only the
# packages above.
